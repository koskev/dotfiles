# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/build.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
    - name: Most important Action!
      uses: wimpysworld/nothing-but-nix@687c797a730352432950c707ab493fcc951818d7
      with:
        hatchet-protocol: rampage
    - name: Install nix
      uses: cachix/install-nix-action@4e002c8ec80594ecd40e759629461e26c8abed15
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: cachix/cachix-action@ad2ddac53f961de1989924296a1f236fcfbaa4fc
      with:
        name: koskev
    - name: Check flake
      run: nix flake check
    - name: Build NixOS for kevin-nix
      run: nix run nixpkgs#nixos-rebuild -- --flake .#kevin-nix build --accept-flake-config
    - name: Build NixOS for kevin-laptop
      run: nix run nixpkgs#nixos-rebuild -- --flake .#kevin-laptop build --accept-flake-config
    - name: Build NixOS for kokev
      run: nix run nixpkgs#nixos-rebuild -- --flake .#kokev build --accept-flake-config
    - name: Build HomeManager for kevin@kevin-nix
      run: NIX_CONFIG="accept-flake-config = true" nix run nixpkgs#home-manager --
        --flake  .#kevin@kevin-nix build
    - name: Build HomeManager for kko@liag0005
      run: NIX_CONFIG="accept-flake-config = true" nix run nixpkgs#home-manager --
        --flake  .#kko@liag0005 build
    - if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/renovate/lock-file-maintenance'
      uses: cachix/cachix-action@ad2ddac53f961de1989924296a1f236fcfbaa4fc
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: koskev
        signingKey: ${{ secrets.CACHIX_SIGNING_KEY }}
        skipPush: true
    - if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/renovate/lock-file-maintenance'
      run: nix build .#push
    - if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/renovate/lock-file-maintenance'
      name: Push to cachix
      run: nix path-info .#push | cachix push koskev
name: Build Nix Configurations
'on':
  push: {}
